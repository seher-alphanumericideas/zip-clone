specVersion: "v1"
serviceId: "zip-clone"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "zip-clone-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
